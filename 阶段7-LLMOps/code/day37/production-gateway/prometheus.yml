global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'llm-gateway'
    metrics_path: '/actuator/prometheus'
    scheme: http
    static_configs:
      - targets: ['production-gateway:8080']
        labels:
          application: 'production-gateway'
