server:
  port: 8080

spring:
  application:
    name: java-dify

# Dify API config (for Java → Dify calls)
dify:
  api:
    base-url: http://localhost:5001
    api-key: app-xxx  # Will be replaced after Dify workflow is created

logging:
  level:
    com.ai.learning.dify: DEBUG
